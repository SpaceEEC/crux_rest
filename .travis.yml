sudo: false

language: elixir
elixir: '1.6.6'
otp_release: '20.3'

script:
  - mix format --check-formatted
  - mix test --no-start

deploy:
  skip_cleanup: true
  provider: script
  script: bash deploy/deploy.sh
  on:
    tags: true
    branch: master